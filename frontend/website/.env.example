# Base URL of the LunaLens backend API.
# Used by the frontend (Vite) at build/dev time. Override per environment.
# For production, set this to your deployed backend URL.
VITE_API_URL=http://localhost:5000
