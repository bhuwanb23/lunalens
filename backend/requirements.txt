# Flask and Web Framework
Flask>=2.3.0
Flask-CORS>=4.0.0
Flask-SQLAlchemy>=3.0.0
Flask-Migrate>=4.0.0

# Authentication
PyJWT>=2.8.0

# Environment and Configuration
python-dotenv>=1.0.0
Werkzeug>=2.3.0

# Data Processing
numpy>=1.24.0

# Computer Vision (for boulder detection)
opencv-python>=4.8.0
Pillow>=10.0.0

# Note: ML packages (torch, transformers, etc.) are commented out
# as they may have compatibility issues with Python 3.13
# Install them separately if needed for boulder detection 